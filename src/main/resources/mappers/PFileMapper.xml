<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.patent.mapper.PFileMapper">

    <insert id="addFile">
        insert into p_file
        (id, file_name, file_path)
        values
        (#{id},#{fileName},#{filePath})
    </insert>

    <select id="findFileById" resultType="com.example.patent.entity.PFile">
        select id as 'id', file_name as 'fileName', file_path as 'filePath' from p_file where id = #{id}
    </select>

    <select id="pFileCount" resultType="int">
        select MAX(id) from p_file
    </select>

</mapper>